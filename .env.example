# ===================================================
# CONFIGURAÇÃO DE AMBIENTES (DEV & PROD SEPARADOS)
# ===================================================

# Defina para 'production' ou 'development'
NODE_ENV=development

# --- DESENVOLVIMENTO (Local) ---
PORT_DEV=3000
BASE_URL_DEV=http://localhost:3000
API_KEY_DEV=dev_secret_key
SESSION_ID_DEV=zapbridge_session_dev

# --- PRODUÇÃO (Servidor) ---
PORT_PROD=80
BASE_URL_PROD=https://api.seudominio.com
API_KEY_PROD=coloque_uma_chave_muito_longa_e_segura_aqui
SESSION_ID_PROD=zapbridge_session_prod

# --- PADRÕES (Opcional) ---
# Se as variáveis _DEV ou _PROD não existirem, o sistema tentará as abaixo:
PORT=3000
API_KEY=my_super_secret_api_key
SESSION_ID=zapbridge_session
BASE_URL=http://localhost:3000
